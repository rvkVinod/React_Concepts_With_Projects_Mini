import React from "react";

function FunctionComponent(props){
    return(
        <div>
            <h1>using props</h1>
            <h4>im the Function component props={props.wish} today date is props= {props.date} </h4>
        </div>
    )
}

export default FunctionComponent;